<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Green_EV2">

        <!-- Todas las activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"> <!-- Si esto aqui es TRUE es porque es lo PRIMERO que va a aparecer, esta ventana -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- Es la actividad del inicio -->
                <category android:name="android.intent.category.LAUNCHER" /> <!-- aparecerá en el launcher del teléfono -->
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".crear_cuenta"
            android:exported="false" />
        <activity
            android:name=".recuperar_Cuenta"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>